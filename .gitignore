# Build / dependencies
node_modules/
dist/
dist_update_*/
dist_offline_*/
out/
build/
!build/installer.nsh
*.log
logs/
scripts/logs/

# Framework caches
.cache/
.next/
%TEMP%/

# Local profiles / data
profiles/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Runtime artifacts (Local export / shots)
media/
shots/
poc/media/
poc/shots/
poc/logs/

# OS
.DS_Store
# Windows NTFS Alternate Data Streams (Zone.Identifier metadata)
# Git on Windows encodes the colon as U+F03A (PUA, EF 80 BA in UTF-8)
*:Zone.Identifier
*Zone.Identifier

# Local tools (do not commit)
tools/bfg*.jar

# Secrets / env
.env
.env.*
/.secrets/
scripts/.github_token

# IDE / local configs
.cursor/
.agent/

# Kameleo / Playwright local files
fingerprint.json
running_profiles.json
node_modules_old/
playwright-core-*.tgz
poc/node_modules/
swagger.json
swagger_index.html
logs.txt
 